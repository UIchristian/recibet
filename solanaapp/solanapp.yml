info:
  name: solanapp
  type: http

http:
  method: GET
  url: http://localhost:3001/api/games/18241006
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
